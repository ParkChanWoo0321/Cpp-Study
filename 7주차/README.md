# 📘 07.cpp - C++ 클래스와 구조체 실습 정리

이 파일은 C++에서 구조체와 클래스 개념을 포인터와 함께 실습한 내용을 담고 있습니다. 다양한 객체지향 개념을 직접 구현하고 테스트한 코드들을 포함하고 있으며, 주요 학습 포인트는 다음과 같습니다:

---

## ✅ 주요 학습 내용 요약

### 1. 구조체 포인터 사용 (`namecard`)
- 구조체 배열을 포인터로 순회하며 데이터 입력 및 출력.
- 구조체 멤버 접근 시 `->` 연산자 활용.

### 2. 클래스 기본 생성자/생성자 오버로딩
- 생성자에서 멤버 초기값을 설정.
- 동일한 이름의 클래스 생성자를 매개변수 유무에 따라 다르게 정의.

### 3. 멤버 함수 내에서 값 계산
- `getArea()`, `inquiry()` 등 멤버 함수를 통해 내부 상태 값 연산 수행.

### 4. 생성자 한 줄 초기화 및 정사각형 판별
- `width = height = n` 식으로 동일한 초기값 설정 가능.
- `isSquare()`를 통해 정사각형 여부 판별 구현.

### 5. 클래스 소멸자 사용
- 객체 소멸 시 자동 호출되는 소멸자 `~ClassName()` 정의.
- 리소스 정리 혹은 종료 로그용으로 활용.

---

## 🛠️ 실습 클래스 목록

- `namecard` (구조체 + 포인터)
- `Circle`, `Rectangle`, `Oval` (도형 면적 계산)
- `Tower`, `Date`, `Integer` (단순 멤버 접근/출력)
- `Account` (계좌 관리: 입금, 출금, 잔액 조회)
- `CoffeeMachine` (상태 변화 + 리필 기능 포함)

---

## 💡 실습을 통해 익힌 개념

- 구조체와 클래스의 차이점
- 포인터와 배열의 관계
- 객체 생성/소멸 흐름 이해
- 클래스 캡슐화(private/public) 및 생성자/소멸자 구조
- 객체를 활용한 상태 추적 및 변경 방법

---

## 📁 파일 정보

- **파일명:** `07.cpp`
- **언어:** C++
- **구성:** 약 10개 이상의 클래스/구조체 예제
- **목적:** 포인터, 생성자, 멤버함수, 캡슐화, 소멸자 등 객체지향 기초 익히기

---

## 📌 추가 참고 사항

- 모든 클래스는 `main()`에서 직접 생성하여 테스트됨
- 입출력은 `std::cin`, `std::cout` 활용
- 복잡한 라이브러리 없이 순수 C++ 기초 문법으로만 작성
