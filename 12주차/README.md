# 🔷 12.cpp - C++ 가상함수, 추상클래스, 다형성 실습

`12.cpp` 파일은 C++의 **다형성(Polymorphism)**과 **가상 함수(Virtual Function)**, **추상 클래스(Abstract Class)** 개념을 실습하기 위한 예제 코드 모음입니다.  
서로 다른 클래스들이 동일한 함수 이름을 구현하더라도, 어떤 객체를 통해 호출하느냐에 따라 다른 동작을 하도록 구성되어 있습니다.

---

## ✅ 주요 학습 내용 요약

### 1. 📐 추상 클래스(Abstract Class)와 순수 가상 함수
- `AbstractGate`, `Shape` 클래스는 `virtual ... = 0` 형태의 순수 가상함수를 포함하고 있어 인스턴스화 불가능
- 각각의 파생 클래스(ANDGate, ORGate, XORGate 등)에서 해당 함수들을 오버라이딩하여 실제 기능 구현

### 2. 🧠 가상 함수 기반 다형성
- `Shape*` 배열을 통해 다양한 도형 클래스(Oval, Rect, Triangular)의 넓이 계산을 하나의 인터페이스로 호출
- 함수 이름은 같지만 객체 타입에 따라 실행 결과가 달라지는 **동적 바인딩(dynamic binding)** 실습

### 3. 🧮 논리 게이트 클래스 설계
- `AbstractGate` 클래스를 기반으로 `ANDGate`, `ORGate`, `XORGate` 구현
- `set()` 함수로 불리언 입력 설정 후, `operation()`으로 결과 반환

### 4. 🧱 동적 메모리와 소멸 처리
- `Shape* p[3] = new XXX()` 형식으로 도형 객체들을 생성하고, 반복문으로 순회하며 넓이 계산 및 이름 출력
- 종료 시 모든 객체를 `delete`하여 동적 메모리 해제

---

## 🧪 포함된 클래스 목록

| 클래스 이름     | 설명 |
|----------------|------|
| `AbstractGate` | 논리 게이트 추상 클래스 |
| `ANDGate`, `ORGate`, `XORGate` | 각 논리 연산을 구현한 게이트 |
| `Shape` (추상) | 도형의 공통 속성 (이름, 너비, 높이) |
| `Oval`, `Rect`, `Triangular` | 도형별 넓이 계산 방식 구현 |

---

## 🧠 학습 포인트

- `virtual` 키워드를 이용한 함수 재정의와 동적 호출
- 추상 클래스 → 순수 가상함수만 선언하고 구체적 구현은 파생 클래스가 담당
- 포인터 배열을 통한 다양한 객체 호출
- 코드 재사용성과 확장성을 고려한 다형성 설계

---

## 🗂️ 파일 정보

- **파일명:** `12.cpp`
- **언어:** C++
- **특징:** 논리 게이트, 도형 넓이 계산, 추상클래스 기반 설계

---
